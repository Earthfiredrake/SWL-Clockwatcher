<Window x:Class="Clockwatcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Clockwatcher"
        xmlns:ikrv="clr-namespace:IKriv.Windows.Controls.Behaviors"
        mc:Ignorable="d"
        Title="Mission Cooldowns" Height="350" Width="300">
    <Window.Resources>
        <local:TimeFormatConverter x:Key="timeFormatter" />
    </Window.Resources>
    <Grid Margin="0,0,0,3">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TabControl Margin="10,10,10,5" Grid.ColumnSpan="2"
                    ItemsSource="{Binding CharacterMissionLists}" SelectedIndex="0"
                    ikrv:TabContent.IsCached="True">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding CharName}"/>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <ikrv:TabContent.Template>
                <DataTemplate DataType="{x:Type local:CharacterTimers}">
                    <DataGrid ItemsSource="{Binding TimerList}" CanUserAddRows="False" CanUserDeleteRows="False" RowDetailsVisibilityMode="Collapsed" AutoGenerateColumns="False" IsReadOnly="True" KeyboardNavigation.TabNavigation="None">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Name}" Header="Timer" IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding RemainingTime, Converter={StaticResource timeFormatter}}" Header="Time Left" SortDirection="Ascending" IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="IsHitTestVisible" Value="False"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Class}" Value="{x:Static local:TimerClass.AgentMission}">
                                        <Setter Property="Background" Value="#FF90A9CD" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Class}" Value="{x:Static local:TimerClass.AgentRecovery}">
                                        <Setter Property="Background" Value="#FFEA796D" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Class}" Value="{x:Static local:TimerClass.Lair}">
                                        <Setter Property="Background" Value="#FFB790C9" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>
                </DataTemplate>
            </ikrv:TabContent.Template>
        </TabControl>
        <Button Content="Refresh" Width="75" Grid.Row="1" Command="{Binding RefreshCommand}" HorizontalAlignment="Center"/>
        <Button Content="Clear Ready" Width="75" Grid.Row="1" Command="{Binding ClearReadyCommand}" HorizontalAlignment="Center" Grid.Column="1"/>
    </Grid>
</Window>
